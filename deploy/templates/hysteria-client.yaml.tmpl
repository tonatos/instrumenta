# Generated by deploy bootstrap from inventory hysteria2_uri — do not commit.
# Hysteria accepts a full hysteria2:// URI as `server` (auth + sni included).

server: "{{ .ServerURI }}"

http:
  listen: 0.0.0.0:8080

socks5:
  listen: 0.0.0.0:1080
